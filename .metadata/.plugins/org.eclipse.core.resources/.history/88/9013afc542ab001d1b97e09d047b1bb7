<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
<link rel="stylesheet" type = "text/css" th:href="@{bootstrap.min.css}">


</head>
<body>
   <h1>질문 테이블의 리스트를 출력하는 View 페이지 입니다.</h1>
   <hr>
   
   <div class="container my-3">
   <table class="table">
   		<thead class = "table-dark">	<!-- 제목을 처리 -->
	      <tr>
	      	<th>번호</th>
	        <th>제목</th>         
	        <th>작성일시</th>
	      </tr>
      </thead>
      
      <tbody> 	<!-- 테이블의 내용을 출력 블락 -->
      
      <!-- 타임리프(Thymeleaf)를 사용해서 루프 돌려서 출력
   <tr th:each= "question: ${questionList}">
      <td>
      	<a th:href="@{|/question/detail/${question.id}|}" th:text= "${question.subject}"></a>
      </td>  
      <td th:text= "${question.createDate}"></td>
   </tr>
    	
    -->
    <!-- Thymeleaf에서 링크를 처리 : @{} -->
    <!-- | String + 변수 | 문자열과 변수를 연결 
    
    
    <!-- 방법2 (위랑 똑같은거임) 	   -->
   <tr th:each= "question, Loop : ${questionList}">
   
   	<td th:text="${Loof.count}">번호</td>
      <td ><a th:href="@{|/question/detail/${question.id}|}">[[${question.subject}]]</a></td>      
      <td>[[${question.createDate}]]</td>
   </tr>
	
  	 </tbody>
   </table>
   
   </div>

</body>
</html>