<html layout:decorate="~{layout}">


<div layout:fragment="content" class="container my-3">
	<table class="table">
		<thead class="table-dark">
			<!-- 제목을 처리 -->
			<tr>
				<th>이름</th>
				<th>이메일</th>
				<th>작성일시</th>
			</tr>
		 <a th:href="@{/users/create}" class="btn btn-primary" style="margin-bottom:50px">회원가입</a>	
		</thead>

		<tbody>
		

			<!-- 타임리프(Thymeleaf)를 사용해서 루프 돌려서 출력
   <tr th:each= "question: ${questionList}">
      <td th:text= "${question.subject}"></td>
      <td th:text= "${question.content}"></td>
      <td th:text= "${question.createDate}"></td>
   </tr>
    	-->

			<!-- 방법2 (위랑 똑같은거임) -->
			<tr th:each="users: ${usersList}">
				<td><a th:href="@{|/users/detail/${users.idx}|}">[[${users.name}]]</a></td>
				<td>[[${users.email}]]</td>
				<td>[[${users.createDate}]]</td>
			</tr>

		</tbody>

	</table>



</div>

</html>